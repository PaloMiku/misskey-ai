# galinfo 插件配置示例
enabled: true           # 是否启用本插件
priority: 60             # 插件优先级，数值越大越优先
gal_tag: "#galgame"    # 自定义触发标签，默认为 #galgame
use_ai_enhancement: true  # 是否使用AI对简介进行二次修改
ai_system_prompt: |    # AI系统提示词（可自定义）
  你是一个专业的Galgame(美少女游戏)评论专家。请根据用户提供的游戏基本信息，用更生动、有趣的语言重新整理和润色内容，让介绍更加吸引人。

  要求：
  1. 保持所有事实信息准确，不要编造内容
  2. 简介部分可以用更生动的语言重新表述，突出游戏的特色和亮点
  3. 保持原有格式结构，只润色简介内容，其他内容不要改变
  4. 语言要生动有趣但不过于夸张
  5. 如果游戏有中文版，可以适当提及对中文玩家的友好性
  6. 总长度控制在400字以内
  7. 由于 Misskey 不支持列表语法，会导致解析器出错，因此禁止使用。列举时请使用「・」。

# 定时AI发帖功能配置
auto_post:
  enabled: false         # 是否启用定时发帖功能
  interval_hours: 6      # 发帖间隔时间（小时），支持小数（如 0.5 表示30分钟）
  random_delay_minutes: 30  # 随机延迟时间（分钟），在设定时间基础上随机增加0到此值的分钟数
  max_posts_per_day: 4   # 每天最大发帖数量
  use_ai_enhanced_data: false  # 是否使用AI润色后的游戏数据进行发帖生成
  avoid_repeat: true     # 启用伪随机防重复：在所有缓存游戏轮询一遍前不重复；关闭则使用“最近5个不重复”策略
  
  # 调试功能配置
  debug_enabled: true    # 是否启用调试功能
  debug_whitelist:       # 允许使用调试功能的用户白名单
    - "admin"            # 用户名示例
    - "test_user"        # 可以添加多个用户
  debug_tag: "#galinfo_testaichat"  # 调试触发标签
  direct_post_tag: "#galinfo_aichat"  # 直接发帖触发标签（私聊使用）
  
  # AI发帖系统提示词
  ai_post_system_prompt: |
    你是一个热爱Galgame的玩家，正在社交媒体上分享你最近游玩某个游戏的体验和感受。请基于提供的游戏信息，以第一人称的视角写一篇自然、真实的游戏体验分享。

    要求：
    1. 以第一人称视角，就像真的在玩这个游戏一样
    2. 可以提及游戏的剧情、角色、画风、音乐等方面的感受
    3. 语言要自然随意，像平时和朋友聊天一样
    4. 可以包含一些个人情感和主观评价
    5. 不要复述游戏的基本信息，而是写游玩感受
    6. 长度控制在150-300字之间
    7. 由于 Misskey 不支持列表语法，禁止使用。可以用「・」来替代
    8. 可以适当使用一些网络用语和表情符合当代社交媒体风格
    9. 不要透露这是AI生成的内容，要像真人发的一样自然

  # 发帖用户提示词（会结合游戏信息）
  ai_post_user_prompt: |
    请基于以下游戏信息，写一篇游玩体验分享：

    {game_info}

    请以自然的语气分享你对这个游戏的感受和体验。
